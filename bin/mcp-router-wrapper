#!/bin/bash
# Wrapper that sources shell profile for env vars before running router

# Source zshrc for OPENAI_API_KEY etc
[ -f ~/.zshrc ] && source ~/.zshrc 2>/dev/null

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$SCRIPT_DIR/mcp-router" "$@"
